# FS-i6 → Arduino Leonardo → ПК (симулятор) — инструкция

Цель: подключить пульт **FlySky FS-i6** с приёмником **FS-IA6B** к ПК через **Arduino Leonardo** и управлять авиасимулятором (например, World of Aircraft) как USB-джойстиком с минимальной задержкой.

## Принцип работы

1. Пульт FS-i6 передаёт сигнал на приёмник FS-IA6B.
2. Приёмник выдаёт все 6 каналов одним цифровым пакетом по протоколу **iBUS** (последовательный порт, 115200 бод), обновление ~7 мс.
3. Leonardo читает этот поток через `Serial1` (пин 0 / RX) и разбирает значения каналов.
4. Leonardo прикидывается USB-геймпадом (библиотека Joystick): оси X/Y/Z + Throttle + 2 кнопки.
5. Симулятор видит обычный джойстик.

Почему iBUS, а не ШИМ: ШИМ-выходы обновляются раз в 20 мс (50 Гц), iBUS — раз в ~7 мс. Это убирает главный источник задержки.

Раскладка каналов (Mode 2, стандарт FS-i6):

| Канал | Функция | Орган управления | Куда в джойстике |
|---|---|---|---|
| CH1 | Aileron (крен) | правый стик ←/→ | ось X (Roll) |
| CH2 | Elevator (тангаж) | правый стик ↑/↓ | ось Y (Pitch) |
| CH3 | Throttle (газ) | левый стик ↑/↓ | Throttle |
| CH4 | Rudder (рыскание) | левый стик ←/→ | ось Z (Yaw) |
| CH5 | переключатель SWA | тумблер | Кнопка 1 |
| CH6 | переключатель SWB | тумблер | Кнопка 2 |

## Компоненты

- Пульт FlySky FS-i6
- Приёмник FS-IA6B
- Arduino Leonardo
- Провод-перемычка (dupont «мама-мама») — один сигнальный провод для iBUS
- ПК на Windows с авиасимулятором

## Подключение

| Приёмник FS-IA6B | Arduino Leonardo |
|---|---|
| GND (пин «-») | GND |
| VCC (пин «+», 5V) | 5V |
| Порт «SERVO» (iBUS), сигнальный пин | D0 (RX) |

Важные моменты:

- Порт iBUS — это **3 горизонтальных пина сбоку платы**, подписанные **«SERVO»**. Не путать с двумя рядами каналов CH1–CH6 и с портом «SENS» (он для датчиков/телеметрии, нам не нужен).
- Из трёх пинов нужен только **сигнальный** (крайний; средний = +5V). GND и 5V приёмник уже получает по отдельным проводам.
- Питание приёмника — от 5V Leonardo (потребляет ~40 мА, хватает от USB).
- Пульт FS-i6 настраивать не нужно: iBUS на порту «SERVO» включён по умолчанию после привязки.

## Установка библиотек

В Arduino IDE: **Sketch → Include Library → Manage Libraries**, найти и установить:

1. **Joystick** (автор Matthew Heironimus)

(Библиотека `EnableInterrupt` больше не нужна — ШИМ-прерывания убраны.)

## Настройка пульта (FS-i6)

1. Проверить режим **Mode 2** (по умолчанию).
2. **OK (зажать 1 с) → Functions setup → Aux. channels**:
   - **Ch5 = SWA**
   - **Ch6 = SWB**
   - (по умолчанию на CH5/CH6 часто стоят крутилки VrA/VrB — их надо заменить на тумблеры)
3. Проверка: **OK → Functions setup → Display** — щёлкаем SWA/SWB, полосы 5 и 6 должны двигаться.

## Привязка приёмника (bind)

1. На пульте зажать кнопку **BIND** (слева внизу) и включить пульт.
2. На FS-IA6B замкнуть перемычкой контакты **B/VCC** (bind) при подаче питания.
3. После успешной привязки светодиод приёмника горит постоянно. Перемычку убрать.

## Прошивка Leonardo

1. **Tools → Board → Arduino Leonardo**, выбрать COM-порт.
2. Открыть `FLYSKY_v1.ino` и нажать **Upload**.
3. При «зависании» на `Connecting to programmer: .` — нажать **Reset** на плате и повторить.

## Проверка в Windows

1. Win+R → `joy.cpl` → Свойства.
2. Подвигать стики: крестик (X/Y), слайдер Z (левый стик ←/→).
3. Тумблеры SWA/SWB — загораются Кнопка 1 и Кнопка 2.
4. Throttle в стандартном окне `joy.cpl` **не отображается** — это норма, проверяется в игре.

## Настройка симулятора (World of Aircraft)

1. Подключи Leonardo **до запуска игры** (геймпад должен определяться — проверь в `joy.cpl`).
2. Запусти игру, открой **Options / Settings → Controls / Input** (точные названия меню могут немного отличаться).
3. В списке устройств выбери контроллер — обычно отображается как **Arduino Leonardo** или **Gamepad / Joy 1**.
4. Назначение осей — кликни на строку действия и подвигай нужный стик до упора:
   - **Roll / Aileron (крен)** → правый стик ←/→ (ось X)
   - **Pitch / Elevator (тангаж)** → правый стик ↑/↓ (ось Y)
   - **Yaw / Rudder (рыскание)** → левый стик ←/→ (ось Z)
   - **Throttle (газ)** → левый стик ↑/↓ (ось Throttle)
   - **Кнопки** (шасси, флапы и т.п.) → тумблеры SWA/SWB (Кнопка 1 / Кнопка 2)
5. Проверь реакцию: подвигал стики — в игре должен двигаться соответствующий индикатор/самолёт.
6. Если ось реагирует «наоборот» — включи для неё **Invert** (инверсия).
7. При наличии пункта **Calibrate / Deadzone** — откалибруй и настрой мёртвую зону.
8. Если после этого остаётся заметная задержка — уменьши сглаживание/фильтр входа (input smoothing), если игра его предлагает.

Замечания:

- В Unity-играх ось часто назначается только после полного хода стика — двигай до упора.
- **Throttle** — это отдельная ось «Throttle», не путать с осью Z.
- Если игра не видит устройство — перезапусти игру (геймпад должен быть подключён до её запуска).

## Диагностика

В скетче есть флаг `DEBUG` (в начале файла):

```cpp
const bool DEBUG = true;  // true = вывод значений каналов и частоты обновления в Serial Monitor
```

При `DEBUG = true` в **Tools → Serial Monitor** (скорость 115200) каждые ~0.5 с выводится строка:

```
CH1..CH6: 1500 1500 1000 1500 1000 1000 | frames=68 interval=7.1ms (136/s) | B1=0 B2=0
```

- `CH1..CH6` — значения каналов (мкс).
- `frames` — сколько валидных iBUS-кадров получено за интервал.
- `interval` — время между соседними кадрами (должно быть ~7 мс).
- `B1/B2` — состояние кнопок.

Если `interval` заметно больше 10 мс или `frames/s` заметно ниже 100 — проблема на линии iBUS.

Расшифровка проблем:

| Симптом | Причина |
|---|---|
| В Serial Monitor ничего не выводится / `interval` большой | Провод iBUS не на сигнальном пине порта «SERVO» или не на D0 |
| Значение канала не меняется при движении стика | Не тот канал назначен в игре / стик не откалиброван в пульте |
| CH5/CH6 не реагируют на тумблеры | В пульте Ch5/Ch6 не назначены на SWA/SWB (стоят VrA/VrB) |
| Ось в игре инвертирована | Инвертировать в настройках игры (не в коде) |
| Остаётся задержка в игре | Уменьшить сглаживание/мёртвую зону в настройках входа игры |

## Файлы проекта

- `FLYSKY_v1.ino` — прошивка Leonardo (джойстик по iBUS + диагностика).
